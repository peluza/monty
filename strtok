#include "holberton.h"

void _strtok(char line)
{
    int x;
    char *value;

    str1 = malloc(sizeof (char *));

    value = strtok(line, " \t\n");
    x = 0;
    while (line)
    {
        str1[x] = strdup(value);
        value = strtok(NULL, " \t\n");
        x++;
    }
    free(value);
}